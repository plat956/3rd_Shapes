package by.latushko.training.service;

import by.latushko.training.entity.Rectangle;

public interface RectangleCalculationService {
    int calculateSquare(Rectangle rectangle);
    int calculatePerimeter(Rectangle rectangle);
}
